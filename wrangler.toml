name = "dlp-cc-regex"
main = "src/index.js"
compatibility_date = "2026-09-11"

# Local playground + optional deploy. No bindings needed — it's stateless.
# `npx wrangler dev`     → http://localhost:8787
# `npx wrangler deploy`  → *.workers.dev (add a route/custom domain if you want it behind Gateway)
