{
  "name": "dlp-cc-regex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Rust-regex-validated security pattern builder for Cloudflare DLP custom entries.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-wasm-modules --disable-warning=ExperimentalWarning --test test/*.test.mjs"
  }
}
